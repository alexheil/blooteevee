<%= provide(:title, "Your Plan") %>

<div class="wrapper">
  <div class="container">
    <div class="form-container padding-vertical">

      <div class="padding-bottom center mid-text border-bottom">
        Create a plan
      </div>

      <div>
        You currently do not have a plan. A plan allows users to subscribe to you for a monthly cost. This can be to access premium videos, or simply to support you if all of your videos are free already.
      </div>

      <div class="padding-top">
        <%= form_for(@plan, url: user_plans_path) do |f| %>
          <div class="field">
            <%= f.label :amount, "Amount per month" %>
            <%= f.text_field :amount, class: "text-field" %>
          </div>
          <div class="field">
            <%= f.label :currency %>
            <%= f.select :currency, options_for_select(currency) %>
          </div>
          <%= f.submit "Create plan", data: { disable_with: 'Creating...' }, class: "submit-button" %>
        <% end %>
      </div>

    </div>
  </div>
</div>