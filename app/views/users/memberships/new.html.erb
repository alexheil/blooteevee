<%= form_for(@membership, url: user_membership_path(@user)) do |f| %>
<%= f.hidden_field :membership_type, :value => "basic" %>
<%= f.submit "Create membership", data: { disable_with: 'Creating...' } %>
<% end %>